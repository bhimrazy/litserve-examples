# Pin the rule set explicitly. Ruff's defaults expand between releases, and CI
# installs the latest ruff, so relying on them lets an upstream release turn a
# green main red without a single line of this repo changing.
[lint]
select = ["E4", "E7", "E9", "F", "I", "UP", "B", "SIM", "LOG", "RUF"]
